# 클라이언트 UI 정리 (페이즈3)

브랜치 `dev-phase3`. 1차 여덟 건, 2차 셋, 3차 셋이다.

## 요구 사항

| # | 내용 | 상태 |
|---|---|---|
| 1 | 최상단의 "연결됨", "언어" 를 설정 화면으로 옮긴다 | 완료 |
| 2 | "로그아웃" 을 설정 화면으로 옮긴다 | 완료 |
| 3 | "어드민 패널" 을 설정 화면으로 옮긴다 | 완료 |
| 4 | 설정 화면에 "종료" 를 둔다 | 완료. 웹 빌드에서는 감춘다 |
| 5 | 로그의 기본 탭은 "전체" | 이미 그랬다. 테스트로 고정 |
| 6 | 로그에서 "전투", "이동", "아이템", "사회", "시스템" 탭을 뺀다 | 완료 |
| 7 | "채팅" 과 "대화" 를 한 탭으로 합친다 | 완료. 탭 이름은 "대화" |
| 8 | 로그에 "파티" 탭을 넣는다 | 탭만 완료. 채울 것이 없다 |

## 설계

### 설정 화면

`scenes/settings/settings.tscn` 을 신설한다. 다른 화면과 같은 방식으로 조립
지점(`boot.gd`)이 만들고 `_show_only` 로 전환한다. 팝업이 아니라 화면인 것은
기존 인벤토리·상태 화면과 같은 규약을 쓰기 위해서다.

담는 것은 넷이다.

- 연결 상태 (`ConnectionIndicator` 를 그대로 옮긴다. 재연결 버튼 포함)
- 언어 선택 (`LocaleSelector` 를 그대로 옮긴다)
- 로그아웃
- 어드민 패널 (어드민 계정일 때만 보인다)
- 종료

두 공용 컴포넌트는 노드 위치만 바뀐다. `boot.gd` 가 여전히 `bind` 하고 신호를
받는다. 설정 화면은 그것들을 자식으로 담을 뿐이다.

`main.tscn` 헤더에서 로그아웃과 어드민 버튼을 빼고 설정 버튼 하나를 넣는다.
`boot.tscn` 의 `TopRow` 는 비게 되므로 없애고 `Notice` 만 남긴다.

종료는 `get_tree().quit()` 이다. 웹 빌드에서는 브라우저가 창을 닫지 못하므로
동작하지 않는다. 그 빌드에서는 버튼을 감춘다(`OS.has_feature("web")`).

### 로그 탭

필터가 여덟에서 셋으로 줄어든다.

| 탭 | 담는 것 |
|---|---|
| 전체 | 전부 |
| 대화 | `chat` 메시지와 `event.category == "dialogue"` |
| 파티 | 아직 없음 |

기본은 전체다. 지금도 그렇다.

"채팅" 과 "대화" 를 합치는 것이므로 탭 이름은 "대화" 하나로 둔다. 필터 하나가
두 종류를 받으므로 `_render` 의 비교를 목록 대조로 바꾼다.

빠지는 다섯 탭의 내용은 사라지지 않는다. "전체" 에 그대로 남는다.

### 파티

서버, 계약, 클라이언트 어디에도 파티 기능이 없다. `follow`/`unfollow` 가
있지만 그것은 따라가기이며 파티가 아니다. `event.category` 에도 `party` 가
없다(계약이 정한 여섯은 combat, movement, item, social, system, dialogue).

탭은 요청대로 만들되 채울 것이 없다. 서버가 `party` 분류의 `event` 를 보내기
시작하면 그대로 채워진다. 그때까지 빈 탭이다. 계약에 `party` 를 넣는 것은 서버
쪽 작업이다.

## 작업

- [x] 1. 설정 화면 신설
  - `scenes/settings/settings.gd`, `settings.tscn`
  - 연결 상태, 언어, 로그아웃, 어드민, 종료
  - 어드민 버튼은 `login_result.admin_channel.available` 일 때만 보인다
  - 종료 버튼은 웹 빌드에서 감춘다
- [x] 2. 조립 지점 배선
  - `boot.gd` 가 설정 화면을 만들고 두 공용 컴포넌트를 그 안에서 찾아 `bind`
  - `boot.tscn` 의 `TopRow` 제거
  - 로그아웃과 어드민 신호를 설정 화면에서 받는다
- [x] 3. 메인 헤더 정리
  - `main.tscn` 에서 로그아웃·어드민 버튼 제거, 설정 버튼 추가
  - `main.gd` 의 해당 신호와 텍스트 정리
- [x] 4. 로그 탭 축소
  - `event_log.gd` 의 `FILTERS` 를 셋으로
  - 대화 탭이 `chat` 과 `dialogue` 를 함께 받는다
  - 쓰이지 않는 `ui.log.*` 문구 정리, `ui.log.party` 추가
- [x] 5. 검증
  - `bash scripts/godot-check.sh`, `godot-test.sh`
  - `test_quality.gd` 의 자유 입력 화면 목록에 설정 화면이 들어가지 않는지 확인
    (설정 화면에는 문자 입력이 없다)
  - 실제 서버에 붙여 화면 전환과 로그 탭 동작 확인

## 결과

### 딸려 나온 분리

`boot.gd` 가 설정 화면 배선으로 521행이 되어 500행 상한을 넘었다. 로그인 왕복을
`scenes/login/login_flow.gd` 로 뺐다. `RegisterFlow` 와 같은 구조다. 자격 저장과
자동 로그인, 거절 처리가 그쪽으로 갔고 조립 지점은 475행이 됐다.

`EventLog` 의 필터 판정을 `static func accepts(filter, channel)` 로 뺐다. 탭
구성이 요구 사항이므로 테스트가 붙잡아야 하는데, `TestCase` 가 `RefCounted` 라
씬을 트리에 넣을 수 없어 씬 없이 검증할 형태가 필요했다.

### 테스트

`test_emotes.gd` 의 "로그 필터가 계약의 category 를 모두 덮는다" 는 전제가
바뀌어 무효가 됐다. 넷으로 갈랐다.

| 테스트 | 붙잡는 것 |
|---|---|
| 로그 필터가 셋이다 | 탭 구성 |
| 계약의 category 가 전체에 들어온다 | 탭을 없앤 분류가 사라지지 않는다 |
| 대화 탭이 채팅과 대화를 함께 받는다 | 7번 요구 사항 |
| 파티 탭은 다른 분류를 받지 않는다 | 8번이 다른 분류를 끌어오지 않는다 |

검증: `godot-check.sh` 65개, `godot-test.sh` 179건. 실제 사슬에 붙여 `READY`
까지 확인했다. 화면 조작은 헤드리스로 확인할 수 없다.

### 로그인 화면의 설정 입구

옮기고 나서 드러난 것이다. 언어 선택이 설정 화면으로 들어가면서 로그인 전에는
닿을 수 없게 됐다. 처음 실행한 사람이 영어 화면에서 한국어로 바꿀 방법이 사라진다.

로그인 화면에 설정 버튼을 뒀다. 설정 화면은 인증 상태에 따라 로그아웃과 어드민
버튼을 감춘다. 닫으면 열기 전 화면으로 돌아간다.

### 남은 것

파티 탭은 서버가 `party` 분류의 `event` 를 보내기 전까지 빈 탭이다. 계약의
`event.category` 에 `party` 를 넣는 것이 선행 조건이다.

## 2차 요구 사항

| # | 내용 | 상태 |
|---|---|---|
| 9 | 헤더의 "이름(아이디) 종족" 을 캐릭터 화면으로 | 완료. 캐릭터 화면에 이미 있어 헤더만 없앴다 |
| 10 | 채팅 입력을 대화·파티 탭에서만 동작 | 완료. 3차에서 감추기로 바뀌었다 |
| 11 | 방 인원을 물건 아래 버튼으로, 드나듦을 로그로 | 완료 |

`players_here` 버튼을 없앴다. 방 인원이 항상 버튼으로 보이므로 눌러서 목록을 받을
이유가 없다. 자신은 `(나)` 로 앞에 붙는다. 서버는 방 엔티티에 관찰자 자신을 담지
않는다.

드나듦은 `entity_enter`/`entity_leave` 를 `movement` 분류의 이벤트로 바꿔 남긴다.
저장소에 `entity_entered`/`entity_left` 신호를 더했다. 사본 변화만으로는 "누가
들어왔다" 를 알 수 없었다. 몬스터 로밍은 남기지 않는다.

## 3차 요구 사항

| # | 내용 | 상태 |
|---|---|---|
| 12 | `who` 버튼 제거 | 완료. 플레이어 목록 패널도 함께 지웠다 |
| 13 | 시간·좌표를 미니맵 안으로 | 완료 |
| 14 | 전체 탭에서 채팅을 감추고 빠른 대화를 강화 | 완료. 44종 추가 |

### who 제거의 대가

귓속말 대상이 같은 방 사람으로 줄었다. `who` 가 다른 방 사람의 uuid 를 확보하는
유일한 경로였다. 방을 넘는 귓속말이 필요해지면 대상 확보 경로를 다시 만들어야
한다. 사용자명을 입력받는 방식은 쓰지 않는다.

### 빠른 대화

Wolfenstein: Enemy Territory 의 quick chat 을 카르나스의 중세 판타지로 옮겼다.
기존 `emote` 기제를 그대로 쓴다. 클라이언트가 id 를 보내고 서버가 목록을 검사한 뒤
`emote.<id>.self` 와 `.other` 를 뿌리며 문장은 각자의 언어로 렌더링된다. 자유
문자가 오가지 않으므로 서로 다른 언어를 쓰는 사람끼리도 통한다.

문장은 `{이름}: 문장` 으로 나온다. 채팅 줄과 같은 모양이다. `say` 를 대신하는
자리이기 때문이다. 분류는 `social` 이라 "전체" 탭에 남는다.

| 그룹 | 항목 수 | 원본 |
|---|---|---|
| 동작 | 12 | 기존 감정 표현 |
| 전황 | 10 | Statements |
| 요청 | 8 | Requests |
| 지시 | 8 | Commands |
| 응답 | 14 | Talk + Global + Objectives 일부 |
| 역할 | 4 | Function |

합쳐 56종이다. 화면은 그룹을 먼저 고르고 그 안에서 고른다. 한 목록에 56개를 담으면
고를 수 없다.

옮기지 않은 항목이 있다. 대응물이 없는 것들이다.

- 차량 관련 넷(수리·파괴·호송)
- 건설물 파괴와 건설 착수
- 다이너마이트 해제 (함정 해제로 갈음했다)
- 병과 선언 다섯 (이 게임에는 병과가 없다. "앞에서 싸우겠습니다" 같은 의사 표시로 바꿨다)
- 1차·2차 목표 파괴 (목표 개념이 없다)

Global 그룹이 Talk 를 되풀이하던 항목은 하나로 합쳤다.

### 목록 대조

서버 `EMOTE_IDS` 와 클라이언트 `Emotes.IDS` 가 어긋나면 화면에 버튼이 있는데
눌러도 `INVALID_PARAMS` 로 거절된다. `scripts/check-contract-coverage.py` 가 두
저장소의 목록을 읽어 순서까지 대조한다. GDScript 테스트는 그룹이 전체 목록을
빠짐없이 덮는지와 번역 키의 짝을 본다.

검증: godot-check 통과, godot-test 184건, 계약 커버리지 통과(56종 일치). 실제
서버에 다섯 종을 보내 `emote.<id>.self` 가 오는 것과 모르는 id 가
`INVALID_PARAMS` 로 거절되는 것을 확인했다. 서버는 pytest 370, mypy, ruff 통과.
